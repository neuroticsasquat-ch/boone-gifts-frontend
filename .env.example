VITE_API_URL=https://boone-gifts-api.localhost

# Sentry release tag. In Cloudflare Pages this is injected at build time from the
# commit SHA (VITE_GIT_SHA=$CF_PAGES_COMMIT_SHA npm run build). Leave unset locally.
# VITE_GIT_SHA=
